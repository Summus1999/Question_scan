# Question Scan 的代理说明

在这个仓库里工作时，默认遵守这份说明。

## 项目资料

- 产品需求在 `prd/prd-question-scan.md`。
- 开发任务在 `tasks/tasks-question-scan.md`。
- 固定开发流程在 `docs/development-workflow.md`。

在改代码或改实现相关文档之前，先读这三份文件。

## 产品范围

Question Scan 是一个面向授权练习、自测、开放题目环境和个人工作流使用的 Tauri 桌面工具。

允许的工作：

- 用户主动触发的截图。
- 题目区域裁剪。
- 临时图片生成。
- 把图片发给用户配置的多模态 AI 服务。
- 输出 C++ 和其他主流语言的算法解法。
- 用户可控的设置、缓存清理和可选历史。

不要添加下面这些能力：

- 隐蔽考试使用。
- 监考规避。
- 屏幕共享规避。
- 用窗口隐藏来规避检测。
- 反监控行为。
- 自动提交到第三方平台。
- 自动把答案填进第三方网页。
- 读取浏览器 Cookie、账号或凭据。

## 任务流程

把 `tasks/tasks-question-scan.md` 当成唯一任务来源。

规则：

- 一次只做一个子任务。
- 没有得到用户确认前，不开始下一个子任务。
- 每完成一个子任务，马上把它标成 `[x]`。
- `相关文件` 要保持准确。
- 发现新任务就补进任务清单，不要藏在别处。
- 某个父任务下的所有子任务完成后，先验证，再把父任务标成 `[x]`。

## 验证流程

每完成一个子任务，先跑最快、最相关的验证。

完成一个父任务时：

- 如果前端已配置，就跑 `npm test -- --run`。
- 如果存在 `src-tauri`，就从 `src-tauri` 目录跑 `cargo test`。
- 如果项目里存在 E2E 命令，就跑 E2E。
- 按 `docs/development-workflow.md` 做要求的手动验证。

如果没有 E2E 命令，不要说 E2E 已通过。要明确说明阻塞点或者缺失的环境。

## Git 流程

开始前先看：

```bash
git status --short --branch
```

推荐的开发分支：

```bash
feature/question-scan-mvp
```

只有在一个父任务完成并且验证通过后，才允许提交。

提交要用规范化格式：

- `feat:` 表示新功能。
- `fix:` 表示修复。
- `test:` 表示测试相关改动。
- `docs:` 表示纯文档改动。
- `chore:` 表示配置和维护。
- `refactor:` 表示不改变行为的重构。

提交前先跑：

```bash
git diff --check
git status --short
```

只暂存当前任务相关的文件，不要把无关的用户改动一起提交。

提交完成后，把分支推送到对应远端。

## 实现优先级

MVP 的开发顺序如下：

1. Tauri 应用底座。
2. 全局快捷键和托盘流程。
3. 屏幕截图和临时图片链路。
4. 手动框选兜底。
5. AI 多模态请求流程。
6. C++ 直接输出解法。
7. 主流语言模板。
8. 结果面板、复制动作和输出速度。
9. 隐私、可选历史和缓存清理。
10. Windows 打包和发布文档。

先做手动框选，再做自动题目识别。手动框选是第一条可靠主链路，不能被自动识别卡住。
